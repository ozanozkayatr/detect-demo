# Prompt Assets

Local prompt templates will live in this directory.

Recommended convention:

```text
prompts/
└── templates/
    ├── README.md
    └── *.md
```

The backend is already configured to scan `prompts/templates/` for future prompt files.

